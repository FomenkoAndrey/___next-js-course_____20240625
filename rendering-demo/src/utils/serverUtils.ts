import 'server-only'

export const serverUtils = () => {
  console.log(`
    Use server libraries!
    Use server APIs!
    Use environment variables!
    Use a database!
    Use confidential information!
  `)
  return 'Server result'
}
